[project]
name = "px_xtras2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.6",
    "pandas>=2.3.2",
    "plotly>=6.3.0"
]

[tool.hatch.build.targets.sdist]
include = [
  "px_xtras2"
]
exclude = [
    "demo",
    "images"
  ]